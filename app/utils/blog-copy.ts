/**
 * The blog's one-line identity, shared by the index page heading and the RSS
 * channel so the two never drift apart.
 */
export const BLOG_TITLE = "Notes & field reports.";
export const BLOG_TAGLINE =
  "Things I’ve learned the slow way — written down so the next person (or future me) finds them faster.";
